matrix:
- name: Markdown
  hunspell:
    d: en_US.ISO8859-15
  dictionary:
    wordlists:
      - ${VAL_REPO}/lpot_dict.txt
    output: ${VAL_REPO}/lpot_dict.dic
  sources:
  - ${SCAN_REPO}/docs/*
  - ${SCAN_REPO}/*.md
  - ${SCAN_REPO}/deep_engine/**/*.md
